<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laxi.dao.UserMapper">
    <sql id="columns">
        <if test="true">
            id,username,PASSWORD,salt,email,TYPE,STATUS,activation_code,header_url,create_time
        </if>
    </sql>
    <insert id="insertUser">
        INSERT
        INTO USER
        (username, PASSWORD, salt, email, TYPE, STATUS, activation_code, header_url, create_time)
        VALUES (#{username}, #{password}, #{salt}, #{email}, #{type}, #{status}, #{activationCode}, #{headerUrl},
                #{createTime})
    </insert>

    <update id="updateStatus">
        UPDATE USER
        SET STATUS= #{status}
        WHERE id = #{id}
    </update>
    <update id="updateHeader">
        UPDATE USER
        SET header_url= #{headerUrl}
        WHERE id = #{id}
    </update>
    <update id="updatePassword">
        UPDATE USER
        SET password= #{password}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultType="com.laxi.pojo.User">
        SELECT
        <include refid="columns"></include>
        FROM USER
        WHERE id=#{id}
    </select>

    <select id="selectByName" resultType="com.laxi.pojo.User">
        SELECT
        <include refid="columns"></include>
        FROM USER
        WHERE username = #{username}
    </select>
    <select id="selectByEmail" resultType="com.laxi.pojo.User">
        SELECT
        <include refid="columns"></include>
        FROM USER
        WHERE email = #{email}
    </select>
</mapper>

